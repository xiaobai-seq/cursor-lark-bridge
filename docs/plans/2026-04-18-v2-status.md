# V2 执行进度

> 由 Planner 维护。每次 Generator/Evaluator 回报后更新。状态机：
> `pending → in_progress → done_pending_review → done | rework`

## 当前阶段

- Phase: DONE（V2 全部 15 task 完成）
- Active Task: — (plan finished)
- Updated At: 2026-04-18

## Worktree

- main: `/Users/bytedance/repos/cursor-lark-bridge` on `main`
- work: `/Users/bytedance/work/cursor-lark-bridge-v2` on `feat/v2-ops-and-slash`

## Checkpoints

| ID  | 状态    | 说明 |
|-----|---------|------|
| CP1 | passed  | Plan 已获批，路线 A（保留 Hook 模型 + 运维硬化 + 斜杠命令） |
| CP2 | pending | P0 启动前展示 launchd plist 设计决策 |
| CP3 | passed | --real 重跑验证：daemon 版本=本分支 e2e-20260418-215148；崩溃恢复 reconnect_count 3→4；JSON pid schema / launchd KeepAlive / restore 全通过 |
| CP4 | displayed（推进中） | /status 卡片样式按 Plan 默认；有异议可中断修改 |
| CP5 | passed | 用户在飞书实测 8 条斜杠命令（/ping /status /stop /help + 中文别名）全部按预期响应；事件订阅 lark-cli 状态健康 |
| CP6 | authorized | 用户选 C：先恢复 v0.1.4 备份 → F.2 merge+tag（本地不 push）→ 后续 install.sh 部署正式 v0.2.0 |

## Tasks

| ID        | 描述                                           | 状态                  | 提交 / 备注 |
|-----------|------------------------------------------------|-----------------------|-------------|
| SETUP.0   | worktree + docs/plans 初始化                    | done                  | 本次 commit |
| P0.1      | launchd plist + bridge.sh service 子命令        | done                  | worktree commit 6159043; evaluator pass |
| P0.2      | daemon 按天滚动日志 + 7 天保留                  | done                  | worktree commit c71bf09; evaluator pass |
| P0.3      | daemon.pid 扩展为 JSON                           | done                  | worktree commits 7abd651 + b8f6a5f (rework); evaluator pass |
| P0.4      | supervisor 指数退避 2s→300s                     | done                  | worktree commit 9a296e1; evaluator pass |
| P0.5      | P0 e2e + README + 状态更新                      | done                  | worktree commits f9bba50 + ba20238 (CP3 follow-up fix); smoke 绿 |
| P2.1      | pendingEntry 补 kind/summary/workspace/ts + snapshot | done              | worktree commit d2b0f9e; evaluator pass; -race 25 绿 |
| P2.2      | 4 个 hook 脚本补发 metadata                      | done                  | worktree commits 68ce717 + 2323cbe (rework); evaluator pass |
| P2.3      | Slash Router 框架 + /ping                        | done                  | worktree commit b7f4644; 29 test -race 绿; evaluator pass |
| P2.4      | /status 卡片实现                                 | done                  | worktree commit 5019476; 36 test -race 绿; 7 个新测试 |
| P2.5      | /stop 向 chan 写 deny/skip                       | done                  | worktree commit 4ac0411; -race 43 绿; evaluator pass |
| P2.6      | /help + 中文别名 /状态 /停止 /帮助 /指令         | done                  | worktree commit ad0a0e1; 主会话实测 -race 47 绿 + smoke 绿; evaluator pass |
| P2.7      | P2 e2e + README 斜杠命令小节                     | done                  | worktree commit 9b0d3db; 主会话 e2e 退出 0; evaluator pass |
| F.1       | CHANGELOG v0.2.0                                | done                  | worktree commit 5efa5b7; evaluator pass |
| F.2       | merge --no-ff + tag v0.2.0 + 清理 worktree      | done                  | main e97f198 (merge commit); tag v0.2.0 本地已打; worktree + feat 分支已清理 |

## Evaluator Reports

### P0.1 (commit 6159043) — verdict: pass
- 7/7 spec 条目全通过
- 测试覆盖：10 项静态校验（模板、plutil-lint、plistlib 结构断言、bridge.sh 语法）
- Backlog（不阻塞，后续任务顺手处理）：
  1. `sleep 2` 改循环退避风格
  2. `cmd_service_start` 未 load 时自动 load 或友好提示
  3. macOS-only 守护（`uname = Darwin` 检查）
  4. `$HOME/Library/LaunchAgents/*.bak.*` 备份改到 `$BRIDGE_DIR/backups/`
  5. `grep -E '\s'` 改 `[[:space:]]`（POSIX 风格）
  6. `cmd_service_install` 43 行可拆分（可选）
  7. P0.5 e2e 时顺手给 `fb doctor` 加 launchd 接管检查

### P0.2 (commit c71bf09) — verdict: pass
- 12/12 spec 条目全通过
- 测试：5 单测全绿（rotate/append/cleanup/legacy-migrate/idempotent），P0.1 无 regress
- Backlog：
  1. `attachStandardLog` 目前是 dead code（P0.2 保留 / P0.3+ 清理）
  2. `cleanupOldLogs` err != nil 路径目前静默
  3. 考虑容错 MultiWriter 替代 `io.MultiWriter(stderr, dl)`
  4. 补 `setupLogging` 端到端集成测试

### P0.3 (commits 7abd651 + b8f6a5f) — verdict: pass (after rework)
- 初审发现 bridge.sh 6 处 `cat "$PID_FILE"` 未替换（会在 JSON 格式下破坏 fb kill / fb doctor --fix）
- Rework 补齐所有 6 处 + `kill_daemon` 空 PID 分支处理 + `cmd_doctor --fix` 从"写单行 PID"改为"运行 fb restart"
- 新增 `tests/read-pid-from-file-test.sh`（5 类输入）锁定 regression
- 最终 9 个 Go 测试（pidfile）+ 5 个 Go 测试（logger）+ 10 项 service-install-test + 5 项 read-pid-from-file-test 全绿

### P0.4 (commit 9a296e1) — verdict: pass
- 12/12 spec 条目全通过，19 个 Go 测试全绿
- Backlog:
  1. `ReconnectCount`（pid 文件，尝试前 +1）和 `restartCount`（内存，cmd.Start 成功后 +1）语义 drift；P0.5 e2e 观察，必要时把 pid 写入挪到 cmd.Start 成功后
  2. backoff 日志可加 `Current()` 预览
  3. `TestBackoffEdgeCases` 可拆成两个独立 test

### P0.5 (commit f9bba50 + ba20238 follow-up) — verdict: pass (after CP3 fix)
- 9/9 spec 通过；smoke 模式主会话 Shell 实测退出 0 (7 个 step 全绿)
- CP3 用户实测发现 2 个问题，已由 ba20238 修复：
  1. 脚本 python3 对 legacy 单行 PID（`json.load` 返回 int）触发 AttributeError → 抽出 `read_reconnect_count_from_pid` helper 做类型兼容
  2. --real 测的是 install.sh 装的 v0.1.4 而非本分支 daemon → 改为备份后 rebuild + deploy 本分支（版本打 `e2e-<ts>` tag），并加 `/health.version` 断言硬失败如果部署没生效
- Backlog:
  1. `real_backup` 时序：先 cp plist → unload → cp daemon.pid 可能漏备 daemon.pid（建议先双 cp 再 unload）
  2. 第二次 --real 被中断后遗留检测（极端场景；经验法则：跑前 `fb service uninstall`）
  3. `real_restore` cp 失败时 BACKUP_DIR 清理

### P2.1 (commit d2b0f9e) — verdict: pass
- 11/11 spec；go test -race 25/25 全绿；hook 契约完全保留
- Backlog:
  1. P2.2 起让 hook 显式传 summary 并做脱敏（`password/token/secret/key`）
  2. P2.4 卡片让用户可按 request_id 精确定位（诊断 dispatchTextReply FIFO 漂移）
  3. 补 dispatchButtonReply 删除后 snapshot 为空的 test
  4. P0.4 遗留：ReconnectCount vs restartCount drift 继续观察

### P2.2 (commits 68ce717 + 2323cbe rework) — verdict: pass
- 所有 hook 补发 kind/summary/workspace + 脱敏（api_key/password/token/secret/bearer）
- Rework 修：shell summary 80 字封顶、on-stop summary 防御性脱敏、mcp 反向测试锁意图
- Planner 裁定：mcp JSON 正则不加 lone `key`（误伤率高），mcp summary（=tool_name）不脱敏
- Backlog:
  1. 3 处脱敏正则字面相同，可抽 `hooks/_sanitize.py` 共享
  2. `tests/hook-metadata-test.sh` §8 是源码拷贝，P2.7 考虑用 bash -x 抓真实 body
  3. shell `[^\s]+` 对 quoted value 右引号过贪婪（展示层，不影响功能）
  4. mcp summary 不脱敏可补一行决策注释

### P2.3 (commit b7f4644) — verdict: pass
- 13/13 spec；-race 29 全绿；SlashCommand 接口 + slashRegistry + /ping
- 关键设计落地：未识别命令也返回 true（避免误入 pending FIFO）、全角 `／`、CJK 别名保留
- Backlog:
  1. slash_test.go 自造 contains/indexOf，可用 strings.Contains 替代
  2. `pingCommand.Execute` 测试可追加 `version=` / `uptime=` 字段断言
  3. `normalizeSlash` 对多层前导斜杠（`//ping`）走"未识别"，/help 文案可示意

### P2.4 (commit 5019476) — verdict: pass
- 8/8 spec；-race 36/36 绿；7 个新 test（metadata/icon/line/line_optional/empty/with_pending/truncation）
- Generator 细节：用 `json.Encoder + SetEscapeHTML(false)` 避免 `&/<` 转义影响可读性（飞书客户端非浏览器，关闭安全）
- Backlog:
  1. `formatPendingLine` 在 P2.5 `/stop` 里复用（避免 drift 出两套格式）
  2. 统一排序帮手 `sortPendingByAgeAsc` 避免重复 cmp 闭包
  3. P0 smoke 沙盒限制（heredoc 不可写）是长期限制，真实机器不受影响

### P2.5 (commit 4ac0411) — verdict: pass
- 9/9 spec；-race 43 全绿；7 个新 test（metadata/empty/mixed-kinds/race-skip/empty-card/with-items/race-hint）
- 关键设计：写锁 Lock / non-blocking send / 不 delete pending / 复用 formatPendingLine
- HTTP /stop 语义零触碰（v1 on-stop.sh 仍工作）
- Backlog:
  1. `sort.Slice` 闭包在 stopAllPending + buildStatusCard 重复，可抽 `sortPendingByAgeAsc` helper
  2. buildStopCancelCard 底部 note 引用 /help（P2.6 前会生效）
  3. race default 分支单测由人造 chan 预填覆盖；未来真实路径（如超时 deny）需覆盖

### P2.6 (commit ad0a0e1) — verdict: pass
- 9/9 spec；-race 47 全绿；主会话 Shell 实测 smoke 通过
- SlashCommand 接口加 Description()；helpCommand + /帮助 + /指令 别名
- Evaluator dump 时遗留的 zz_dump_test.go 已在主会话清理
- Backlog:
  1. buildHelpCard body 首行 "可用命令" 与 header 重复，可删
  2. alias 分隔符 " / " 与前导 `/` 视觉黏连（/帮助 / /指令），可改 " · "
  3. TestBuildHelpCardContent 对含 `"`/`\` 的 Description 会 FAIL（未来约束）
  4. /help 没有 arg 解析（P3 观察需求）

### P2.7 (commit 9b0d3db) — verdict: pass
- 7/7 spec；主会话实测 bash tests/p2-e2e.sh 退出 0（9 step）
- 4 张卡片样例（空/有 pending/取消/help）已 commit 供 CP5 预览
- README 中英双语新增"飞书斜杠命令"小节
- Backlog:
  1. p2-e2e.sh 的 `--version` 占位 step 可等 P3 实装或直接删
  2. 样例 JSON 是单行压缩（粘到 cardkit 会格式化，可接受）
  3. p2-e2e.sh 的"静态 registry 检查"是 grep 4 条；P3 扩容可抽 diff 式

### F.1 (commit 5efa5b7) — verdict: pass
- 14/14 commit 覆盖；Keep-a-Changelog + SemVer 双链接；向前兼容章节超出 spec
- Backlog（不阻塞，F.2 前可选补）:
  1. Internal 段可显式提 README 两语言新增 "Auto-start + crash recovery (launchd)" + "Feishu slash commands" 小节
  2. [0.1.5] 节可加日期 + GitHub Releases 链接，避免"git log 指引"突兀
  3. /stop 卡片的 "race skip" 语义未在 CHANGELOG 直接提（影响极小）

## Agent 协作记录

（每次派发 Generator/Evaluator 子 Agent 的时间、task id、摘要）
